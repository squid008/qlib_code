# 数据源接入说明

本文档记录项目的数据源架构、E:\rq 数据目录结构，以及如何接入 rqalpha 数据源。

## 一、数据源架构

项目通过 `app/datasource/factory.py` 统一管理数据源，任何数据源都继承 `app/datasource/base.py` 的 `DataSource` 抽象基类，并实现统一的数据读取接口。

```
                 ┌─────────────────────────────┐
  前端/回测引擎  │   get_data_source(name)      │
                 └──────────────┬──────────────┘
                                │
                 ┌──────────────▼──────────────┐
                 │       DataSourceFactory     │
                 └───┬────────────┬───────────┘
                     │            │
          ┌──────────▼──┐   ┌─────▼──────────┐
          │ QlibDataSource │   │ RQAlphaDataSource │
          └──────────┬──┘   └─────┬──────────┘
                     │            │
               qlib cn_data      E:\rq bundle h5
```

### 数据源能力声明（capabilities）

| 能力 | qlib | rqalpha |
|------|------|---------|
| daily（日线） | ✅ | ✅ |
| minute（分钟） | ❌ | ✅ |
| financial（财报） | ❌ | ✅ |
| industry（行业） | ❌ | ❌（bundle 无独立行业文件） |
| index_constituent（指数成分） | ❌ | ✅ |

- **qlib**：只有日线行情，其余数据不包含。
- **rqalpha**：日线/分钟/财报/指数成分均可读，行业分类暂不可用。

> 注：前端回测界面已隐藏 rqalpha 选项。**回测引擎当前仅支持 qlib 数据源**（依赖 qlib 数据目录格式 DatasetH/D.features/D.calendar）。rqalpha 数据源已接入**数据查询 API**，供因子研究/数据浏览使用，但尚未桥接进回测引擎。

## 二、E:\rq 数据目录结构（实测）

```
E:\rq
├── bundle\                          # rqalpha bundle 主数据
│   ├── stocks.h5                    # 股票日线（5523 只）
│   ├── indexes.h5                   # 指数日线（7014 个）
│   ├── dividends.h5                 # 分红送转
│   ├── trading_dates.npy            # 交易日历（int32，YYYYMMDD）
│   ├── instruments.json / .pk       # 证券列表
│   ├── split_factor.h5              # 拆股因子
│   ├── h5\equities\*.h5             # 分钟线（单股一文件，data+index）
│   ├── h5\future\                   # 期货
│   ├── h5\option\                   # 期权
│   └── ticks\                       # tick 数据
├── finance\
│   ├── pit\*.h5                     # 财报（单股一文件，fields 394 个字段）
│   └── forecast\                    # 业绩预测
└── constituents\
    ├── index\*.h5                   # 指数成分（一指数一文件）
    └── etf\                         # ETF 相关
```

### 各 h5 内部结构

**stocks.h5 / indexes.h5（日线）**
- key = 代码（`000001.XSHE` / `000001.XSHG`）
- 字段：`datetime(i8,YYYYMMDDHHMMSS)`、`open`、`close`、`high`、`low`、`prev_close`、`limit_up`、`limit_down`、`volume`、`total_turnover`

**bundle/h5/equities/{code}.h5（分钟线）**
- `data`：分钟结构化数组（`datetime/open/high/low/close/volume/total_turnover/num_trades`）
- `index`：`[date(YYYYMMDD), line_no]`，每日起始行号，用于切出每天 240 条分钟

**finance/pit/{code}.h5（财报）**
- `info_date`：公告日期（bytes `YYYY-MM-DD`）
- `quarter`：报告期（bytes `YYYYqN`）
- `fields`：Group，394 个财务字段，每字段一个 Dataset（如 `revenue`/`net_profit`/`total_assets`/`roe` 等）

**constituents/index/{code}.h5（指数成分）**
- `change_dates`：成分调整日列表
- `components`：Group，每个调整日一个子组存成分代码列表
- `weights`：三个并行数组 `date`/`order_book_id`/`weight`（含全量历史调整记录）

### 代码格式

| 场景 | 格式示例 |
|------|---------|
| bundle 文件内 | `000001.XSHE`、`000001.XSHG` |
| 对外统一（base.py 契约） | `sz000001`、`sh600000` |

## 三、配置

环境变量（或在 `app/config.py` 修改默认值）：

```bash
# rqalpha bundle 根目录（存 stocks.h5/indexes.h5/h5/trading_dates.npy）
set RQALPHA_BUNDLE_PATH=E:\rq\bundle
# 财报/成分目录（可选，默认在 bundle 同级自动推导）
set RQALPHA_FINANCE_DIR=E:\rq\finance\pit
set RQALPHA_CONSTITUENTS_DIR=E:\rq\constituents\index
```

项目 `app/config.py` 已做默认值探测：若检测到 `E:\rq\bundle` 存在则自动启用 rqalpha 数据源，无需手动配置。

## 四、依赖

读取 h5 需要：

```bash
pip install h5py
```

（qlib 环境已安装。若从零部署需执行。）

## 五、数据查询 API 示例

```bash
# 列出数据源及能力
GET /api/data-sources

# 股票日线（rqalpha）
GET /api/data/daily-bars?instrument=sz000001&start_date=2023-01-01&end_date=2023-01-31&source=rqalpha

# 指数日线
GET /api/data/daily-bars?instrument=sz000300&start_date=2023-01-01&end_date=2023-01-31&source=rqalpha

# 分钟线（1min）
GET /api/data/minute-bars?instrument=sz000001&start_dt=2023-01-04&end_dt=2023-01-04&freq=1min&source=rqalpha

# 财报
GET /api/data/financial?instrument=sz000001&start_date=2022-01-01&end_date=2023-12-31&source=rqalpha

# 指数成分（返回最新一期）
GET /api/data/index-constituents?index_code=sz000300&source=rqalpha
```

## 六、注意事项

- **不要全量读取**：E:\rq 总量约 150G。所有读取都是按需（指定代码/日期区间），按股票/指数/财报单文件访问。
- **`.mmap` 文件不要碰**：`E:\rq` 下如有 `.mmap` 文件是同事的 Java 内存加速实现，不属于 rqalpha bundle 数据，勿读写。
- **指数成分查询**返回最新一期成分+权重（该文件含全量历史调整记录，最多数百万行，已做向量化过滤）。
- **行业分类**：bundle 无独立行业文件，`get_industry` 返回不可用。
- **回测引擎**：当前仅 qlib 数据源可用。若想用 rqalpha 数据跑回测，需后续实现「rqalpha → qlib 数据格式桥接」（大工程，见下）。

## 七、回测引擎数据源抽象

`app/engine/qlib_engine.py` 的 `run_backtest` 通过 `_resolve_data_source(req)` 走数据源工厂：
- `data_source=qlib`（默认）：走 qlib 引擎（现有逻辑）。
- `data_source=rqalpha`：返回清晰错误「回测引擎当前仅支持 qlib 数据源」。
- `data_source=未知`：返回可用数据源列表。

## 八、将来接入 rqalpha 回测的路线（未实施）

1. 实现「rqalpha → qlib 数据格式」adapter，把 E:\rq 的日线/复权转成 qlib `D.features` 可读的格式。
2. 让 `run_backtest` 在 `data_source=rqalpha` 时切换到该 adapter 数据路径。
3. 处理 benchmark/复权/交易日历对齐。
