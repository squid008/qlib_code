# 依赖与构建产物
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
__pycache__/
*.pyc

# 数据（A股行情，体积大，不纳入版本库）
data/
qlib_bin.tar.gz

# 回测运行产物 / 实验记录
mlruns/
backend/mlruns/
backend/workdir/*
# ★ v1.20.65：公式库改为**按人分文件**（`formulas/<user>.json` ✓）⇒ git 层面只新增文件、
#   天然无冲突 ✓✓（原先所有人共用 `custom_formulas.json`，两人各存一次就必然 JSON 冲突 ✗）
!backend/workdir/formulas/
!backend/workdir/formulas/*.json
# ⚠ 老的单文件 `custom_formulas.json` **不再跟踪** ✗（它曾是所有人的共享文件：一旦推送，
#   `pull` 就会把别人本地的公式覆盖掉 ✗）。它仍在本机存在、仍被**只读兜底**读取 ✓，
#   并在首次运行时把内容**迁移**进自己的 `formulas/<user>.json` ✓（见 `ensure_migrated` ✓）。
# ★★ v1.20.69 起改为 `formulas/<装机ID>.json`（与用户名无关 ✓）—— ⚠ 装机 ID 文件
#   `backend/workdir/.install_id` **绝不能入库** ✗✗：一旦随 git 传到另一台机器，
#   两台机器就会写**同一个文件**，又回到"整文件 JSON 冲突"✗。它被上面的
#   `backend/workdir/*` 排除 ✓（下面不再放任何 `!` 例外 ✓）。
**/mlflow.db
*.db

# AI 临时探查文件（覆盖率产物；⚠ v1.20.69 起**锚定根目录** `/workdir/` ——
# 原先写成不锚定的 `workdir/`，会**连带**匹配 `backend/workdir/` 并盖掉上面
# `!backend/workdir/formulas/*.json` 的例外（gitignore「最后匹配者胜」）
# ⇒ 按装机分文件的公式实际**不会被跟踪** ✗（2026-09-24 实测 ✓）
/workdir/
.coverage

# 日志
*.log
# 重启轮转的旧日志（server.log.20260908-234728 之类）
server.log.*
server_err.log.*

# 临时/测试文件
test1.py
test2.py
*.pkl

# AI 临时验算文件（探查/诊断脚本、输出等，可随时安全删除，不入版本库）
ai_test/

# TypeScript 编译缓存
*.tsbuildinfo

# 本地专用文档（不上传到 GitHub）
md/upload.md
# ⚠ md/开发记录.md 已于 v1.19.1（2026-09-13 用户要求）**移出忽略列表**、随仓库上传，
#   便于两地同步（此前为本地专用，需手动拷贝）。

# IDE 项目数据（记忆/规则/自动化等，本地专属，不上传）
.codebuddy/
