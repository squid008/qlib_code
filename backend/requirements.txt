# 后端依赖
# 注意：pyqlib / lightgbm / xgboost 等已随 qlib 环境安装，此处列出的为核心 Web 依赖。
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.0
numpy
pandas
pyarrow

# 交易信号测试：上传 XLSX / XLS 需要的 Excel 引擎（v1.19.46 起）
# ⚠ 本机开发环境早就有它们，CI 的 pip 列表里原先没有 ⇒ 2026-09-15 那次 push CI 直接红
#   （`ModuleNotFoundError: No module named 'openpyxl'`）。运行时缺失只会得到友好报错，
#   但**依赖必须声明**，别只在本机装着。
openpyxl>=3.0        # .xlsx
xlrd>=2.0            # .xls（老格式）

# 回测模型（qlib 环境已装，如缺失可取消注释）
lightgbm>=3.0

# ---- 可选：接入 rqalpha h5 数据时需要的依赖 ----
# h5py
# tables
# rqalpha
# rqdatac
