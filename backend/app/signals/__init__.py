# -*- coding: utf-8 -*-
"""交易信号测试（v1.19.38）：把**外部信号**当 0/1 公式来测 —— 事件研究 + 等权持有回测。

为什么单开一个包（而不是塞进 `factors/single_test.py`）：
  `factors/` 那套是"因子表达式 → 面板求值 → 统计"，而这里的输入是**别人给的 CSV**
  （同事的买入信号清单 / 聚宽的成交明细），输入形态、口径约束（涨跌停能不能成交、持有期
  刷新、资金约束）与命令行/任务模型都不同。混在一起必然变成屎山 —— 所以：
    · 本包**只复用 `factors/` 的公开工具**（`event_study`、`benchmark_curves`），不反向依赖；
    · `factors/` 一行不改（回测页/单因子测试的行为逐位不变）。

两种模式（由 CSV 内容自动识别，用户不用选）：
  ① **信号清单模式**（同事格式 `日期,标的`）：
     事件研究（逐 k 1..N 的触发/非触发/超额曲线 + 锚点）**+** 简单回测（出信号等权买入、
     持有 N 日后卖出、有信号刷新持有期、涨停/停牌买不进、跌停/停牌卖不出顺延、成本可调）。
  ② **交易流水模式**（聚宽 15 列成交明细）：
     **只出净值**（不做 0/1 统计）：A 模拟它反推的费率 / B 用我设定的 cost / C 用它的成交价
     + 手续费精确记账 / + 基准曲线，四条一起对比。

成本语义（与 `engine/cost_curves.py` 的连续信号**完全同义**，避免"同一个 0.004 两个意思"）：
  `cost` = **往返（买+卖）合计费率** ⇒ 单边 = `cost/2`。

价格口径（用户要"前复权"）：
  统一用 `$close`（数据原生**后复权**）—— 同一序列的后复权/前复权只差一个常数比例，
  **收益率逐位相同**、天然含分红，且不会像前复权那样随最新日期漂移。
"""
